[project]
name = "local-llm-video-captioning-mlx"
version = "0.1.0"
description = "Local MLX-powered video captioning backend environment."
requires-python = ">=3.11"
dependencies = [
  "mlx-vlm[torch]",
]

[tool.uv]
package = false
